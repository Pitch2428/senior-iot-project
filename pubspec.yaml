name: sleep_ble_logger
description: "Sleep BLE Logger"
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  flutter_reactive_ble: ^5.4.0
  permission_handler: ^11.3.1

  sqflite: ^2.3.3+1
  path: ^1.9.0
  path_provider: ^2.1.4

  share_plus: ^10.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
